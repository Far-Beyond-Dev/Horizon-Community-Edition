// N.B. The socket.on() stuff in main.rs's on_connect() function should be moved here.
// for reasons relating to data types, this is not done yet, but should be done before wednesday,
// but is not strictly required because the latter works.

